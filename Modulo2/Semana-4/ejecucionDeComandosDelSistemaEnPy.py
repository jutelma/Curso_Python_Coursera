"""
Revisión: Ejecución de comandos del sistema en Python

Estos bloques de código le brindarán la oportunidad de ver cómo está escrito el código, le permitirán practicar su
ejecución y pueden usarse como referencia para consultar.


Import subprocess.run(["date"])

import subprocess.run(["date"])
subprocess.run(["sleep", "2"])

import subprocess.run(["date"])
subprocess.run(["sleep", "2"])
result = subprocess.run(["ls", "this_file_does_not_exist"])
print(result.returncode)


Revisión: Ejecución de comandos del sistema en Python

Estos bloques de código le brindarán la oportunidad de ver cómo está escrito el código, le permitirán practicar su 
ejecución y pueden usarse como referencia para consultar. 

Puede seguir la lectura mientras el instructor analiza el código o revisar el código después de ver el video.


Import subprocess.run(["date"])

import subprocess.run(["date"])
subprocess.run(["sleep", "2"])

import subprocess.run(["date"])
subprocess.run(["sleep", "2"])
result = subprocess.run(["ls", "this_file_does_not_exist"])
print(result.returncode)
"""
# ¿Un comando del sistema que envía paquetes ICMP se puede ejecutar dentro de un script utilizando cuál de los siguientes?
#
#
# R subprocess.run
