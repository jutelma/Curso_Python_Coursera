# Fuentes de recursividad adicionales

# En videos anteriores, visitamos los conceptos básicos de funciones recursivas.
# Una función recursiva debe incluir un caso recursivo y un caso base. El caso recursivo vuelve a llamar a la función,
# con un valor diferente. El caso base devuelve un valor sin llamar a la misma función.

# Una función recursiva normalmente tendrá esta estructura:

# ef recursive_function(parameters):
#   if base_case_condition(parameters):
#       return base_case_value
#   recursive_function(modified_parameters)
